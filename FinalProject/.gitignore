# Eclipse IDE 관련 파일
/.project          # Eclipse 프로젝트 파일
/.classpath        # Eclipse 클래스 경로 파일
/.settings/        # Eclipse 설정 파일
.idea/             # IntelliJ IDEA 설정 파일 (만약 사용하는 경우)

# Eclipse 빌드 디렉토리
/bin/              # Eclipse 빌드 출력 디렉토리
/target/           # Maven 빌드 출력 디렉토리

# Maven 관련 파일
pom.xml.*          # pom.xml 관련 백업 파일들
*.log              # 로그 파일들

# Eclipse 관련 임시 파일들
.metadata          # Eclipse 메타데이터 파일
tmp/               # 임시 파일들
*.tmp              # 임시 파일
*.bak              # 백업 파일
*.swp              # Vim, Emacs 등 편집기 임시 파일
*~.nib             # Mac에서 생성되는 임시 파일
local.properties   # Eclipse 로컬 프로퍼티 파일
.loadpath          # Eclipse 로드 경로 설정 파일
.recommenders      # Eclipse 추천 설정 파일

# Eclipse 외부 툴 빌더 설정
.externalToolBuilders/  # Eclipse 외부 툴 빌더 설정

# Eclipse 런치 설정 파일
*.launch           # Eclipse 런치 설정 파일

# 다른 IDE 관련 파일들
*.pydevproject     # PyDev 프로젝트 파일 (Python 개발을 위한 Eclipse 플러그인)
.cproject          # CDT 프로젝트 파일 (C/C++ 개발을 위한 Eclipse 플러그인)
.autotools         # CDT 자동화 툴 설정
.factorypath       # Java Annotation Processor 관련 파일
.buildpath         # Eclipse 빌드 경로 설정 파일
.target            # sbteclipse 플러그인 관련 파일
.tern-project      # Tern 플러그인 관련 파일
.texlipse          # TeXlipse 플러그인 관련 파일
.springBeans       # SpringBeans 관련 파일
.recommenders/     # Eclipse 추천 설정 파일
.apt_generated/    # Java annotation processor가 생성한 파일
.apt_generated_test/  # 테스트용 annotation processor 파일

# Java 컴파일된 클래스 파일
*.class            # 컴파일된 Java 클래스 파일

# 패키지 및 압축 파일
*.jar              # JAR 파일
*.war              # WAR 파일
*.nar              # NAR 파일
*.ear              # EAR 파일
*.zip              # ZIP 파일
*.tar.gz           # TAR.GZ 파일
*.rar              # RAR 파일

# 웹 애플리케이션의 WEB-INF/lib 디렉토리 안의 JAR 파일은 예외로 처리
!**/WEB-INF/lib/*.jar  # JAR 파일을 예외로 포함

# Eclipse Spring Boot Tooling 캐시
.sts4-cache/      # Spring Tool Suite 4 캐시 파일

# Windows 시스템 관련 파일들
Thumbs.db          # Windows 썸네일 캐시 파일
Thumbs.db:encryptable  # 암호화된 Thumbs.db 파일
ehthumbs.db        # Windows에서 생성하는 썸네일 파일
ehthumbs_vista.db  # Windows Vista 썸네일 파일
*.stackdump        # 스택 덤프 파일
[Dd]esktop.ini     # Windows 바탕화면 구성 파일
$RECYCLE.BIN/      # Windows 휴지통
*.cab              # Windows 설치 파일
*.msi              # Windows 설치 파일
*.msix             # Windows 설치 파일
*.msm              # Windows 설치 파일
*.msp              # Windows 설치 파일
*.lnk              # Windows 바로가기 파일

# Maven Wrapper 관련 파일
.mvn/wrapper/maven-wrapper.jar  # Maven Wrapper JAR 파일

# Maven 빌드 파일들
target/            # Maven 빌드 출력 디렉토리
pom.xml.tag        # pom.xml 태그 파일
pom.xml.releaseBackup  # pom.xml 릴리즈 백업 파일
pom.xml.versionsBackup  # pom.xml 버전 백업 파일
pom.xml.next       # pom.xml 다음 파일
release.properties  # 릴리즈 프로퍼티 파일
dependency-reduced-pom.xml  # 의존성 축소된 POM 파일
buildNumber.properties  # 빌드 넘버 파일
.mvn/timing.properties  # Maven 타이밍 파일

# 업로드된 파일들이 있는 폴더 제외
upload/            # 업로드 폴더 (파일 추적 방지)

